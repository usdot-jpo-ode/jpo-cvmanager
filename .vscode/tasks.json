{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-python-api",
            "type": "shell",
            "command": "${command:python.interpreterPath} -m pip install -r ./api/requirements.txt"
        }
    ]
}
